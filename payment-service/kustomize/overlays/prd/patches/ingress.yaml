apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: payment-service-ingress
  namespace: apps-payment-service
spec:
  rules:
    - host: payment-service.tech.musa.co
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: payment-service-service
                port:
                  number: 3000
  tls:
    - hosts:
        - payment-service.tech.musa.co
      secretName: payment-service-tech-musa-co-tls

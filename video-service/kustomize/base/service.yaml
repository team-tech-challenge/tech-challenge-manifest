apiVersion: v1
kind: Service
metadata:
  name: video-service-service
  namespace: core
spec:
  selector:
    app: hackathon
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
